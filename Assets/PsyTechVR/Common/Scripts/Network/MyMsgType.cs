using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

public class MyMsgType {

	public static short CityParkSceneMsgType = MsgType.Highest + 1; // user messages must be above the highest built-in UNET message id

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
